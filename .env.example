# Database (required for Prisma and the app)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Auth (required for session signing)
NEXTAUTH_SECRET="generate-a-long-random-secret-at-least-32-chars"

# Optional: public app URL (used for API client and password reset links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Required for production tenant subdomains (e.g. abc.supporthubs.org). Leave unset for localhost.
APP_DOMAIN=supporthubs.org
NEXT_PUBLIC_APP_DOMAIN=supporthubs.org

# ---- Email ----
RESEND_API_KEY=re_xxxx
RESEND_FROM_EMAIL="Support Hubs <noreply@yourdomain.com>"
CONTACT_EMAIL=you@example.com
EMAIL_SUPPORT_EMAIL=support@example.com
EMAIL_APP_NAME="Support Hubs"
EMAIL_LOGO_URL=https://yourdomain.com/logo-email.png
EMAIL_COMPANY_NAME=Ordafy
EMAIL_COMPANY_URL=https://yourdomain.com
EMAIL_PRIVACY_URL=https://yourdomain.com/privacy
# SMTP fallback (optional): SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, EMAIL_FROM

# ---- S3 (optional, for logo uploads) ----
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=supporthubs-uploads

# ---- Stripe (optional, for subscriptions) ----
# SUBSCRIPTION_ENABLED=true
# STRIPE_WEBHOOK_SECRET=whsec_xxxx
# STRIPE_SECRET_KEY=sk_xxxx
